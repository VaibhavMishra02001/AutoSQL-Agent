# AutoSQL-Agent
An AI-powered SQL agent that converts natural language queries into executable SQL and runs them on a MySQL database.   Uses an LLM to generate queries and automatically correct errors when execution fails.   Built with LangGraph to manage workflow, decision-making, and retry logic.  
